apiVersion: apps/v1
kind: Deployment
metadata:
  name: gateway
  namespace: jypark
  labels:
    app: gateway
spec:
  replicas: 1
  selector:
    matchLabels:
      app: gateway
  template:
    metadata:
      labels:
        app: gateway
    spec:
      containers:
        - name: gateway
          image: skccuser10.azurecr.io/myInfo:latest
          ports:
            - containerPort: 8080